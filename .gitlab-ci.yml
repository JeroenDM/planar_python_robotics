image: python:latest

before_script:
  - python -V

doc:
  script:
  - pip install sphinx
  - cd docs
  - make html

test:
  script:
  - pip install -r requirements.txt
  - python -m pytest --doctest-modules --cov
  tags:
  - docker
