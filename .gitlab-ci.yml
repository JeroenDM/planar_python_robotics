##image: alpine

image: python:latest
pages:
  script:
  - pip install sphinx
  - cd docs
  - make html
  - mv build/html/ public/
 artifacts:
    paths:
    - public
  only:
    - master
  tags:
  - docker

##before_script:
##  - python -V

##test:
##  script:
##  - pip install -r requirements.txt
##  - python -m pytest --doctest-modules --cov
##  tags:
##  - docker
